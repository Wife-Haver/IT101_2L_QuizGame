# setup for collaboration

1. Clone the repository
2. Create a virtual environment.
   run the following to create a venv (virtual environment):
   ```
   python -m venv .venv
   
4. Install pygame and pillow modules/libraries.
   run the ff:
   ```
   pip install pygame-ce
   pip install pillow
